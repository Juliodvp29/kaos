[package]
name = "kaos"
version = "0.1.0"
edition = "2024"

[dependencies]

# Freestanding binary: no unwinding runtime is available without std.
[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
